binary_sensor.energy_monitor_input_online:
  templates:
    icon_color: >
      if (state === 'on') return 'rgb(0, 180, 30)';
      else return 'rgb(255, 0, 0)';
      
binary_sensor.gas_heater_control_online:
  templates:
    icon_color: >
      if (state === 'on') return 'rgb(0, 180, 30)';
      else return 'rgb(255, 0, 0)';
      
binary_sensor.wall_switch_bedroom_online:
  templates:
    icon_color: >
      if (state === 'on') return 'rgb(0, 180, 30)';
      else return 'rgb(255, 0, 0)';
      
binary_sensor.coridor_light_online:
  templates:
    icon_color: >
      if (state === 'on') return 'rgb(0, 180, 30)';
      else return 'rgb(255, 0, 0)';
      
binary_sensor.outside_sensor_online:
  templates:
    icon_color: >
      if (state === 'on') return 'rgb(0, 180, 30)';
      else return 'rgb(255, 0, 0)';

binary_sensor.magic_light_online:
  templates:
    icon_color: >
      if (state === 'on') return 'rgb(0, 180, 30)';
      else return 'rgb(255, 0, 0)';
      
binary_sensor.diy_wireless_relay_online:
  templates:
    icon_color: >
      if (state === 'on') return 'rgb(0, 180, 30)';
      else return 'rgb(255, 0, 0)';

binary_sensor.gas_heater_relay_online:
  templates:
    icon_color: >
      if (state === 'on') return 'rgb(255, 195, 0)';
      else return 'rgb(0, 153, 255)';
    icon: >
      if (state === 'on') return 'mdi:radiator';
      else return 'mdi:radiator-disabled';

binary_sensor.sonoff_mini_001_online:
  templates:
    icon_color: >
      if (state === 'on') return 'rgb(0, 180, 30)';
      else return 'rgb(255, 0, 0)';

binary_sensor.weather_display_online:
  templates:
    icon_color: >
      if (state === 'on') return 'rgb(0, 180, 30)';
      else return 'rgb(255, 0, 0)';
